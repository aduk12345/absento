# Active Context — eh-absence

## Current Phase: Phase 3 — Implementasi Business Logic
**Objective**: Mengisi halaman placeholder dengan logic sungguhan.
**Status**: Manage Karyawan (CRUD) dan flow check-in/check-out **sudah selesai & terverifikasi end-to-end** (build sukses + test API lengkap: buat karyawan → login → checkin → checkin kedua ditolak → checkout → checkin lagi diperbolehkan). Manage Admin, Manage Absence, Report, History, Profile masih placeholder.

## Recent Changes
- Project Next.js 16 (App Router, TypeScript, Tailwind) di-scaffold via `create-next-app` langsung di root project (bukan subfolder terpisah).
- Dependency terpasang: `firebase`, `firebase-admin`, `cloudinary`, `jose`.
- Lib inti dibuat: `src/lib/firebase-admin.ts` (lazy singleton — penting, jangan diubah jadi eager lagi karena akan gagal saat `next build` collect page data), `src/lib/cloudinary.ts`, `src/lib/session.ts`.
- Auth: custom login via `/api/auth/login` (query Firestore, cocokkan password plain text), session JWT di httpOnly cookie via `jose` — **bukan NextAuth**, keputusan final.
- PWA: manual (bukan `next-pwa`, tidak kompatibel dengan Next.js 16) — `public/manifest.json`, `public/sw.js`, registrasi lewat `RegisterServiceWorker` client component di `layout.tsx`. Icon masih placeholder generated (perlu diganti logo asli).
- Semua halaman route sudah ada sebagai placeholder dengan auth guard aktif (redirect ke `/login` kalau belum login, redirect silang antara area karyawan/admin sesuai role).
- **Manage Karyawan selesai**: `GET/POST /api/employees`, `PATCH/DELETE /api/employees/[id]` + UI `EmployeesTable.tsx` (client component, role-aware — tombol hapus hanya muncul untuk `super_admin`). Pola: server component (`admin/employees/page.tsx`) query Firestore langsung untuk initial data, lalu interaksi lewat `fetch` ke API routes.
- **Field `username` ditambahkan ke `employees`** (`string | null`, alfanumerik saja — divalidasi regex `^[a-zA-Z0-9]+$` di POST & PATCH). Login karyawan sekarang bisa pakai email, username, **atau no. HP** (`/api/auth/login` coba query by email → fallback username → fallback phone). Modal **Edit Karyawan** ditambahkan di `EmployeesTable.tsx` — semua role admin (bukan cuma `super_admin`) bisa mengubah nama/email/username/no. HP **dan** melihat & mengubah password (plain text) — keputusan eksplisit user, beda dari asumsi awal delete-only-super_admin. `GET /api/employees` sekarang mengembalikan field `password` juga (sebelumnya di-strip) karena semua admin boleh melihatnya.
- **Aturan wajib isi Manage Karyawan (keputusan eksplisit user)**: `name` wajib diisi (create & edit, divalidasi server-side di POST dan PATCH — sebelumnya PATCH tidak cek ini sama sekali). `email`/`phone` opsional. Uniqueness dicek untuk email/username/phone masing-masing kalau field itu diisi.
- **Field Username di form Tambah & Edit Karyawan visible, auto-update ikut nama, dan tetap editable manual (keputusan eksplisit user, hasil beberapa iterasi)**: `EmployeesTable.tsx` punya `buildUsernameSuggestion(name)` (helper client-side, logic sama dengan server: kata terakhir nama + suffix 4 digit acak). Username auto ter-regenerate di 3 momen: (1) modal Tambah dibuka (`openAddModal()`), (2) tombol refresh (`RefreshCw` icon) di klik (`regenerateUsername()`/analognya di Edit), (3) **field Nama di-blur** (`onBlur={handleNameBlur}` di form Tambah, `onBlur={handleEditNameBlur}` di form Edit) — ini overwrite username apa pun yang sudah diisi/diedit manual sebelumnya, termasuk di modal Edit (jadi kalau admin ganti nama karyawan lalu klik keluar dari field, username ikut berubah). Admin tetap bisa ketik manual kapan saja setelahnya. `POST /api/employees` terima `username` opsional dari client — kalau diisi divalidasi pattern+uniqueness (409 kalau bentrok), kalau kosong fallback ke `generateUniqueUsername()` server-side (retry 10x).
- **Semua panel "Tambah" di halaman admin pakai `Modal` shared (`src/components/ui/Modal.tsx`), bukan toggle show/hide inline `Card` lagi** (keputusan eksplisit user, berlaku ke semua tempat dengan kebutuhan sama). Diterapkan di `EmployeesTable.tsx` (Tambah Karyawan + Edit Karyawan, dua-duanya sekarang modal), `AdminsTable.tsx` (Tambah Admin), `AbsencesTable.tsx` (Tambah Absen Manual). Pola untuk fitur baru ke depan: pakai `<Modal title="..." onClose={...}>` dari `ui/Modal.tsx`, jangan bikin overlay manual atau toggle `showForm` inline `Card` baru. Catatan: edit-in-place di dalam baris tabel (inline edit row di `AbsencesTable.tsx` & `AdminsTable.tsx`, bukan panel "Tambah") **tidak** diubah ke modal — beda pola UX, di luar scope permintaan ini.
- **Check-in/check-out selesai**: `POST /api/absences/checkin` (create doc, tolak kalau masih ada sesi aktif) dan `POST /api/absences/checkout` (update doc yang `checkoutTime == null`). Halaman `/` query sesi aktif server-side, render `AbsenPanel.tsx` (client component: `getUserMedia` capture via canvas → Geolocation API → upload signed ke Cloudinary → submit ke API).
- **Pola berulang yang dipakai**: halaman admin/karyawan sebagai server component untuk initial data (query `getAdminDb()` langsung), lalu delegasikan interaksi ke client component yang `fetch` API routes. Ikuti pola ini untuk fitur berikutnya (Manage Admin, Manage Absence, Report).

## Aturan Password Minimal 6 Karakter (Ditambahkan)
Rule "password minimal 6 karakter" sekarang ditegakkan di **semua** titik pembuatan/perubahan password, bukan cuma ganti password profile (yang sudah lebih dulu punya validasi ini):
- `POST /api/admins` (buat admin baru) — server-side check ditambahkan, plus client-side check di `AdminsTable.tsx` (`handleAdd`).
- `PATCH /api/employees/[id]` (edit karyawan, field `password` opsional oleh admin) — server-side check ditambahkan, plus client-side check di `EmployeesTable.tsx` (`handleEditSubmit`).
- `POST /api/auth/change-password` — sudah ada validasi ini sebelumnya, tidak diubah.
- **Tidak disentuh**: `POST /api/employees` (buat karyawan baru) — password selalu `DEFAULT_PASSWORD = "123456789"` (9 karakter, sudah otomatis lolos), tidak ada input password manual di form Tambah Karyawan.
- **Pola untuk fitur baru ke depan**: setiap kali ada endpoint yang menerima password baru dari user (bukan default sistem), tambahkan `if (password.length < 6) return 400` di server route DAN validasi cermin di client sebelum submit.

## Report: Foto & Lokasi Dibuka Internal (bukan Link Eksternal)
`ReportView.tsx` diubah supaya link foto/lokasi tidak lagi buka tab baru — sekarang ikuti pola yang sudah ada di `HistoryList.tsx`:
- **Foto**: klik tombol "In"/"Out" buka lightbox internal (`<img>` fullscreen overlay + tombol close, state `zoomPhoto`), bukan `<a target="_blank">` ke Cloudinary URL langsung.
- **Lokasi**: klik tombol "In"/"Out" buka `Modal` (`ui/Modal.tsx`) berisi `LocationMap` (Leaflet, dynamic import `ssr:false`) + link "Buka di Google Maps" (`ExternalLink` icon, `target="_blank"`) sebagai fitur tambahan kalau user tetap mau lihat di Google Maps — state `mapModal` (`{lat, lng, label}`).
- Diterapkan di kedua view (`table` desktop `lg:block` dan `Card` mobile `lg:hidden`).
- **Pola untuk fitur baru ke depan**: kalau ada tampilan lain yang menampilkan foto/lokasi absen (selain `HistoryList.tsx` yang sudah duluan pakai pola ini), ikuti pola sama — jangan link foto/map langsung ke URL eksternal, selalu preview internal + tombol eksplisit "Buka di Google Maps" untuk map.

## Report: Urutan Absence Terbaru di Atas
`GET /api/report` (dipakai `ReportView.tsx`) — `orderBy("checkinTime", "asc")` diubah jadi `"desc"` supaya record absen terbaru tampil paling atas di tabel/list Report. Query masih pakai composite index yang sama (`employeeId` ASC + `checkinTime` DESC, sudah ada dari fitur History), jadi tidak perlu index baru. **Sengaja tidak diubah**: `GET /api/report/export` (Excel export) tetap `asc` — urutan kronologis lebih wajar untuk dokumen laporan yang dibuka orang lain.

## Pagination Manage Karyawan & Manage Absence
`src/components/ui/Pagination.tsx` (BARU) — komponen pagination client-side sederhana (info "Menampilkan X–Y dari Z" + tombol Sebelumnya/Berikutnya), dipakai di `EmployeesTable.tsx` dan `AbsencesTable.tsx`. **Pola**: pagination dilakukan client-side dengan `.slice()` di atas array yang sudah di-fetch penuh (bukan cursor-based Firestore pagination) — sesuai skala data (internal HR tool, ratusan bukan jutaan record). `PAGE_SIZE = 10` di kedua tempat. State `page` di-clamp ke `totalPages` supaya tidak out-of-range setelah delete. **Kalau nanti ada list baru butuh pagination**, pakai ulang komponen ini dengan pola yang sama, jangan bikin komponen pagination baru.

## Filter Tanggal (Rentang) di Manage Absence (default: hari ini)
`GET /api/absences` terima query param `startDate`/`endDate` (`YYYY-MM-DD`, opsional, bisa salah satu saja — yang kosong ikut nilai satunya) selain `employeeId` — filter berdasarkan `checkinTime` dalam rentang tanggal itu (00:00:00.000Z–23:59:59.999Z), bisa dikombinasi dengan `employeeId` (pakai composite index `employeeId` ASC + `checkinTime` DESC yang sudah ada dari fitur History/Report, tidak perlu index baru). **Awalnya sempat dibuat single-date (`?date=`) tapi user eksplisit minta bisa filter lebih dari 1 hari, jadi diganti jadi range** — pola query-nya sama seperti `/api/report`.
`admin/absences/page.tsx` (server component) query awal **hanya hari ini** (bukan lagi `limit(500)` semua histori) dan kirim `defaultStartDate`+`defaultEndDate` (keduanya ISO date hari ini) ke `AbsencesTable.tsx`. `AbsencesTable.tsx` punya 2 input tanggal (Mulai/Akhir, mirip `ReportView.tsx`) + tombol "Terapkan" (fetch eksplisit, bukan auto-fetch tiap ganti tanggal — supaya user bisa set kedua tanggal dulu sebelum query jalan) + tombol "Hari Ini" untuk reset ke default kalau rentang sedang bukan hari ini. Validasi `startDate > endDate` di client sebelum fetch. Hasil fetch di-map jadi `employeeName` pakai daftar `employees` di props (API mentah tidak resolve nama). **Pola untuk fitur baru ke depan**: kalau nambah filter lain (mis. per source/status) di Manage Absence, ikuti pola sama — query param range di API route + tombol "Terapkan" eksplisit di client, bukan fetch-all-lalu-filter-di-client.

## Bug Fix: request `/admin/*` hang tanpa selesai (loading terus) di dev

**Gejala**: refresh halaman `/admin`, `/admin/employees`, `/admin/admins`, atau `/admin/absences` tanpa session valid — response tidak pernah selesai/menutup koneksi (browser loading spinner tidak berhenti), sedangkan `/admin/report`, `/`, dan `/login` normal.

**Root cause**: `admin/layout.tsx` sudah punya guard `redirect()` kalau tidak ada session/bukan admin, tapi 4 dari 5 halaman admin (`admin/page.tsx`, `admin/employees/page.tsx`, `admin/admins/page.tsx`, `admin/absences/page.tsx`) **tidak** punya guard sendiri di level page — mereka cuma mengandalkan redirect dari layout, padahal tetap langsung eksekusi query Firestore tanpa syarat. Hanya `admin/report/page.tsx` yang dari awal punya guard eksplisit (`getSession()` + `redirect()` sendiri sebelum query). Di Next.js 16.2.10 (Turbopack dev), redirect dari layout ternyata tidak selalu menghentikan rendering child page dengan bersih ketika child juga melakukan async data fetching — request-nya nge-hang (connection tidak pernah close, dikonfirmasi lewat raw socket test: `cat <&3` block sampai timeout meskipun kirim `Connection: close`), bukan cuma soal compile lambat.

**Fix**: tambahkan guard eksplisit yang sama (cek `getSession()`, `redirect("/login")` kalau tidak ada session, `redirect("/")` kalau bukan admin session) di keempat halaman itu juga, persis pola `admin/report/page.tsx` — jangan cuma andalkan redirect di layout untuk halaman yang melakukan data fetching sendiri. **Pola wajib untuk halaman admin baru ke depan**: selalu guard session di level page juga, bukan cuma di `admin/layout.tsx`.

## Bug Fix: Service Worker cache stale bikin "Uncaught TypeError: network error" & stuck loading di dev

**Gejala**: setelah login & pakai app beberapa saat (banyak edit + Fast Refresh terjadi), refresh browser → loading lama lalu console error `Uncaught TypeError: network error`, kadang disusul redirect aneh (`GET / 307`).

**Root cause**: `RegisterServiceWorker.tsx` register `public/sw.js` **tanpa cek `NODE_ENV`** — jalan juga di `next dev`. `sw.js` pakai strategi network-first + cache-fallback untuk semua GET non-`/api/*`, termasuk request chunk JS `/_next/static/*` dan RSC navigation payload. Karena Turbopack Fast Refresh terus generate ulang chunk dengan hash baru tiap kali file di-edit (dan sepanjang sesi ini banyak sekali file di-edit), Service Worker yang sudah kadung registrasi dari load sebelumnya nyimpen cache chunk/RSC payload versi lama. Begitu browser butuh chunk versi baru yang direferensikan RSC payload lama itu, request-nya gagal (chunk lama sudah tidak ada) → `network error`, dan browser nyangkut nunggu chunk yang tidak akan pernah datang.

**Fix**: `src/components/RegisterServiceWorker.tsx` sekarang cek `process.env.NODE_ENV === "production"` — cuma register SW di production build. Di dev mode, sebaliknya, **unregister semua SW registration yang ada + hapus semua Cache Storage** (`caches.keys()` → `caches.delete()`) supaya SW nyasar dari sesi/testing sebelumnya (mis. pernah coba `npm run build && npm run start` di browser yang sama) ikut dibersihkan otomatis. **Pola wajib**: service worker/PWA caching **jangan pernah aktif di dev mode** — selalu gate dengan `NODE_ENV === "production"` kalau nambah SW baru atau ubah `sw.js`.

## Update Password Admin (Manage Admin)
`PATCH /api/admins/[id]` sekarang menerima field `password` opsional (validasi minimal 6 karakter, sama pola dengan endpoint lain). Aturan otorisasi baru (beda dari `name`/`role` yang tetap super_admin-only):
- **super_admin**: bisa update password admin manapun (juga tetap satu-satunya yang bisa ubah `name`/`role`).
- **admin biasa**: hanya bisa update password **miliknya sendiri** (`id === session.adminId`), tidak bisa menyentuh admin lain sama sekali — request ke ID lain ditolak 403 sebelum body dibaca.
- Kalau bukan super_admin dan bukan diri sendiri → 403. Kalau field `name`/`role` dikirim oleh non-super_admin, diabaikan (bukan error) — hanya `password` yang diproses untuk mereka.

`AdminsTable.tsx`: kolom "Aksi" sekarang muncul juga untuk admin biasa (bukan cuma `isSuperAdmin`) **kalau** baris tersebut adalah dirinya sendiri (`canEditRow()` helper: `isSuperAdmin || admin.id === currentAdminId`). Baris admin lain untuk non-super_admin tetap tanpa tombol aksi apa pun. Form edit menampilkan input Nama & select Role hanya untuk `isSuperAdmin`; input Password baru (opsional saat isSuperAdmin, wajib diisi saat admin biasa edit dirinya sendiri) selalu muncul untuk siapa pun yang `canEditRow`. Tombol "Hapus" tetap `isSuperAdmin`-only (tidak berubah). **Pola untuk fitur baru ke depan**: kalau ada aksi lain yang campur (sebagian super_admin-only, sebagian self-service), pisahkan gating per-field/per-tombol seperti ini, jangan gate seluruh kolom/baris dengan satu flag `isSuperAdmin` saja.

## Fitur Storage (Menu Admin, Super Admin Only)
- **Menu baru "Storage"** di `AdminNav.tsx`/`AdminMobileNav.tsx` — `ADMIN_NAV_ITEMS` sekarang tiap item punya field `superAdminOnly: boolean`, di-filter di kedua komponen berdasarkan prop `role` yang dilempar dari `admin/layout.tsx` (`session.role`). **Pola untuk item nav baru yang role-gated ke depan**: tambahkan `superAdminOnly` di array, jangan andalkan redirect di halaman saja untuk menyembunyikan link dari sidebar/drawer.
- `src/app/admin/storage/page.tsx` (server component, guard `session.role !== "super_admin"` → `redirect("/admin")`) + `src/components/StorageUsagePanel.tsx` (client component).
- **Pantau pemakaian Cloudinary**: `GET /api/admin/storage-usage` panggil `cloudinary.api.usage()` (Admin API) — dipakai juga langsung di server component untuk initial load. Field kunci: `credits.used_percent` (persentase pemakaian free tier, ditampilkan sebagai progress bar warna dinamis: indigo <70%, amber 70-90%, rose >90%), `storage.usage`/`bandwidth.usage` (bytes, ditampilkan sebagai GB/MB). **Firestore tidak punya endpoint usage% serupa** (butuh GCP Monitoring API terpisah) — sengaja tidak ditampilkan, karena skala data teks absen jauh di bawah limit 1GB free tier (lihat percakapan user soal estimasi storage).
- **Hapus data absensi lama (per bulan/tahun)**: `POST /api/admin/absences-cleanup` (super_admin only) — body `{mode: "month"|"year", year, month?, dryRun}`. `dryRun: true` return `{count}` saja (preview, dipakai tombol "Cek Jumlah Data"), `dryRun: false` benar-benar menghapus: foto Cloudinary (`checkinPhotoUrl`/`checkoutPhotoUrl`) dihapus lewat `deletePhotoByUrl()` per-chunk 20 (bukan `delete_resources_by_prefix` karena folder Cloudinary per-employee, bukan per-bulan-lintas-employee), lalu Firestore docs dihapus via `db.batch()` chunk 500. **Route menolak periode yang belum selesai** (`range.end >= now` → 400) supaya tidak menghapus data bulan berjalan. Satu entry ringkasan (bukan per-record) ditulis ke `absence_audit_logs` dengan `action: "bulk_delete"` — deviasi disengaja dari `logAbsenceAudit()` biasa (per-record terlalu berat untuk ratusan/ribuan record sekaligus).
- **Pola untuk fitur bulk-delete lain ke depan**: ikuti pola dryRun/preview dulu sebelum delete sungguhan (2 request terpisah dari client, bukan langsung hapus dari 1 klik), dan selalu hapus Cloudinary dulu baru Firestore (best-effort, gagal Cloudinary tidak membatalkan hapus Firestore — sama seperti pola hapus 1 record di `/api/absences/[id]`).

## Struktur Folder Cloudinary Dipisah: Foto Absen vs Foto Profil
Folder Cloudinary direvisi (2026-07-14) supaya foto absen (checkin/checkout) tidak bercampur dengan foto profil karyawan:
- Foto absen: `absence/attendance/{employeeId}/{yyyy-MM}/{checkin|checkout}_{timestamp}` (sebelumnya `absence/{employeeId}/{yyyy-MM}/...`, tanpa segmen `attendance/`).
- Foto profil (fitur belum ada UI upload-nya, field `photoUrl` di `employees` baru diterima mentah lewat API, belum ada tempat isi dari client): `absence/employees/{employeeId}/profile_{timestamp}`.
- `src/lib/cloudinary.ts`: `buildAbsencePublicId()` diupdate ke path baru, ditambah `buildProfilePublicId()` baru. `deleteEmployeePhotos()` sekarang hapus KEDUA prefix (`absence/attendance/{employeeId}/` dan `absence/employees/{employeeId}/`) paralel via `Promise.all`.
- `/api/cloudinary/sign` sekarang terima `type: "checkin" | "checkout" | "profile"` (sebelumnya cuma 2 pertama) — kalau `profile`, pakai `buildProfilePublicId()`.
- **Pola untuk fitur foto baru ke depan**: kalau nambah jenis foto lain (bukan absen/profil), buat sub-folder baru di bawah `absence/` (bukan flat di root), dan pastikan `deleteEmployeePhotos()` ikut menghapus prefix baru itu supaya tidak orphan saat karyawan dihapus.
- Detail lengkap: `docs/cloudinary-schema.md`.

## Komponen `PasswordInput` (Toggle Show/Hide)
`src/components/ui/PasswordInput.tsx` (BARU) — wrapper client component di atas `Input`, tambah tombol icon mata (`Eye`/`EyeOff` dari lucide-react) di kanan field untuk toggle show/hide password, default masked (`type="password"`), state lokal `visible` per-instance. Diterapkan di **semua** input password di `/` (karyawan) dan `/admin`, termasuk `EmployeesTable.tsx` (modal Edit Karyawan) — field itu awalnya sengaja dilewati karena sempat pakai `type="text"` (selalu plain visible), tapi user eksplisit minta konsisten dengan menu lain (hidden by default + toggle), jadi diubah juga ke `PasswordInput`. Lokasi lengkap: `login/page.tsx`, `ProfileForm.tsx` (3 field: password lama/baru/konfirmasi), `AdminsTable.tsx` (form Tambah Admin + edit inline desktop & mobile), `EmployeesTable.tsx` (edit password karyawan). **Pola untuk field password baru ke depan**: selalu pakai `<PasswordInput ... />` (bukan `<Input type="password" ... />` maupun `<Input type="text" ... />` untuk password), tidak ada pengecualian.

## Logout Admin & Redirect `/login` Kalau Sudah Login
- **Tombol Logout di area admin** (sebelumnya cuma ada di `ProfileForm.tsx` untuk karyawan) — ditambahkan ke `AdminNav.tsx` (sidebar desktop, tombol di bawah, `mt-auto`) dan `AdminMobileNav.tsx` (drawer mobile, di bawah daftar menu). Keduanya `fetch("/api/auth/logout", {method:"POST"})` lalu `router.push("/login")` + `router.refresh()` — pola sama persis dengan `handleLogout` di `ProfileForm.tsx`.
- **`/login` sekarang redirect kalau sudah ada session aktif**: `src/app/login/page.tsx` diubah dari client component (form langsung) jadi **server component guard** — cek `getSession()`, kalau ada redirect ke `/admin` (pakai `isAdminSession()`) atau `/` (karyawan). Form login lama dipindah utuh ke `src/components/LoginForm.tsx` (client component baru, isi sama, tidak ada perubahan logic). **Pola untuk halaman publik lain ke depan** (kalau ada) yang perlu redirect-jika-sudah-login: pisahkan jadi server component guard (page.tsx) + client component form terpisah, jangan taruh form langsung di page.tsx sebagai client component kalau butuh guard server-side.

## Active Decisions & Considerations
- **Password disimpan plain text untuk versi awal** (keputusan eksplisit user, demi kecepatan development). Ini technical debt yang **wajib** diperbaiki sebelum data karyawan sungguhan dipakai — lihat `techContext.md`.
- Karena custom login (bukan Firebase Auth), **semua akses Firestore wajib lewat API routes** — tidak ada shortcut client-side Firestore SDK untuk data apa pun. Sudah diterapkan konsisten di scaffold (`getAdminDb()` hanya dipanggil dari dalam API routes).
- **Session strategy sudah final**: JWT custom (`jose`) di httpOnly cookie, bukan NextAuth.
- `.env.local` tidak pernah disimpan di repo (di-generate sementara untuk testing lalu dihapus lagi) — pakai `.env.local.example` sebagai template.
- Beberapa hal masih terbuka (lihat "Belum Diputuskan" di masing-masing file docs):
  - Definisi status pada history absen (terkait izin/cuti) — belum dipikirkan.
  - Kebijakan retensi/auto-delete foto lama di Cloudinary — belum diputuskan.

## Next Immediate Steps
- [x] Isi `.env.local` dengan kredensial Firebase (project `absence-dd5b3`, dari service account JSON) — Firestore database sudah dibuat & terverifikasi terkoneksi (test lewat `/api/auth/login`, dapat response 401 "Akun tidak ditemukan" yang menandakan query jalan normal, cuma datanya belum ada).
- [x] Isi kredensial Cloudinary di `.env.local` — sudah terisi, tapi belum terverifikasi konektivitasnya (sandbox development tidak punya akses keluar ke `api.cloudinary.com`, ETIMEDOUT/ENETUNREACH saat test `cloudinary.api.ping()`). Perlu ditest ulang di environment dengan akses internet normal.
- [x] Akun **super_admin** pertama sudah dibuat manual di Firestore (`admins` collection) lewat route seeding sementara (dibuat & dihapus lagi setelah dipakai — tidak ada endpoint seeding publik yang tersisa di kode). Kredensial: username `superadmin`, password di-set manual (tidak dicatat di sini, plain text sesuai technical debt). Login sudah diverifikasi berhasil (`POST /api/auth/login` → `{"role":"super_admin"}`).
- [x] Implementasi API routes `/api/absences/checkin` dan `/api/absences/checkout` — terverifikasi end-to-end via curl.
- [x] Implementasi UI halaman Absen: kamera (`getUserMedia`) + Geolocation API + upload ke Cloudinary via `/api/cloudinary/sign` — kode sudah ada di `AbsenPanel.tsx`, **belum ditest di browser sungguhan** (cuma API-nya yang ditest pakai URL foto dummy).
- [x] Implementasi CRUD Manage Karyawan (API routes + UI form/tabel).
- [ ] Test manual di browser: buka `/login`, login sebagai `budi@test.com` / `123456789`, coba flow check-in dengan kamera & lokasi asli.
- [ ] Implementasi CRUD Manage Admin (khusus super_admin).
- [ ] Implementasi Manage Absence (koreksi + audit log ke `absence_audit_logs`).
- [ ] Implementasi Report + export Excel (sesuai wireframe di `docs/features.md`).
- [ ] Implementasi halaman History (list & detail absen karyawan, read-only, filter per bulan).
- [ ] Implementasi halaman Profile (detail karyawan, ganti password dengan re-auth).
- [ ] User review tampilan Report setelah UI dibuat (masih menunggu, sesuai permintaan awal).

## Rebrand: Absento → SJB Attendance (2026-07-19)
Nama aplikasi diganti dari "Absento" jadi **"SJB Attendance"** (keputusan eksplisit user). Diterapkan di semua tempat nama brand muncul: `src/app/layout.tsx` (`metadata.title`, `appleWebApp.title`), `public/manifest.json` (`name`, `short_name`), `src/components/LoginForm.tsx`, `src/components/EmployeeNav.tsx`, `src/components/AdminNav.tsx`, `src/components/AdminMobileNav.tsx`, `src/app/page.tsx` (header logo + teks). `logo.png` (file gambar) tidak diganti — cuma teks/alt text. Nama repo/package (`eh-absence` di `package.json`) sengaja **tidak** diubah (nama kode/repo, beda dari nama brand — lihat `projectbrief.md`). Verifikasi: `npm run build` sukses (28 route, 0 error). **Pola untuk rebrand berikutnya**: cek titik-titik ini sebagai checklist lengkap kalau nama brand berubah lagi.

## Fitur: Watermark Lokasi & Waktu di Foto Absen (2026-07-19)
Foto checkin/checkout sekarang otomatis dapat watermark ala "GPS Map Camera" (lokasi + koordinat + hari/tanggal/jam) di-bake **permanen ke file foto** (bukan overlay UI) — sesuai contoh yang diberikan user.
- `src/lib/watermark.ts` (BARU) — `drawAbsenceWatermark(canvas, {lat, lng, shortLabel, displayName, date})`: gambar gradient box gelap di bagian bawah canvas + pin icon + teks (nama kota/provinsi/negara, alamat lengkap di-wrap maks 2 baris, koordinat desimal `°N/S °E/W`, dan `hari, dd/mm/yyyy • HH:mm WIB`). Semua ukuran font/padding di-scale relatif `canvas.width / 1280` supaya proporsional di resolusi kamera manapun.
- `src/lib/date.ts` — tambah `formatJakartaWatermarkDateTime()` (format hari Indonesia + dd/mm/yyyy + HH:mm WIB, timezone Asia/Jakarta, konsisten dengan `TIMEZONE` yang sudah ada di file itu).
- `src/app/api/geocode/reverse/route.ts` (BARU) — proxy server-side ke Nominatim (OpenStreetMap) reverse geocoding, **wajib lewat server** (bukan client langsung) supaya bisa kirim `User-Agent` yang valid sesuai usage policy Nominatim. Auth-gated (`getSession()` wajib ada, tidak dibatasi role tertentu). Best-effort: kalau gagal, return `{shortLabel: null, displayName: null}` (bukan error) — watermark tetap tampil pakai koordinat saja.
- `src/components/AbsenPanel.tsx` — `handleStart()` sekarang juga panggil `fetchAddress(lat, lng)` paralel (tidak blocking) begitu lokasi didapat, hasil disimpan di state `address` supaya sudah siap saat user menekan capture. `handleCapture()` panggil `drawAbsenceWatermark(canvas, ...)` **setelah** `drawImage(video)` tapi **sebelum** `canvas.toBlob()` — urutan ini kritis, watermark harus di-bake sebelum convert ke blob supaya ikut ter-upload ke Cloudinary.
- **Pola untuk fitur foto baru ke depan**: kalau ada tempat lain yang capture foto dari canvas (belum ada saat ini, cuma `AbsenPanel.tsx`), reuse `drawAbsenceWatermark()` dari `src/lib/watermark.ts` — jangan duplikasi logic watermark.
- **Update (2026-07-19, iterasi kedua)**: user minta ganti pin polos jadi **screenshot mini-map titik lokasi** (mirip contoh GPS Map Camera) + teks lokasi diperbesar. `drawAbsenceWatermark()` sekarang **async** (dulunya sync) — fetch tile peta itu butuh network round-trip. Detail:
  - `src/app/api/maps/tile/route.ts` (BARU) — proxy server-side ke tile CARTO Voyager (`https://{a-d}.basemaps.cartocdn.com/rastertiles/voyager/{z}/{x}/{y}.png`, basemap sama dengan `LocationMap.tsx`). **Wajib lewat proxy server** (bukan client fetch langsung ke cartocdn.com) — kalau tile diambil cross-origin tanpa proxy, `<canvas>` jadi "tainted" dan `canvas.toBlob()` akan gagal SecurityError, merusak seluruh alur upload foto. Proxy bikin gambar same-origin dari sudut pandang browser, aman untuk canvas.
  - `src/lib/watermark.ts` — `loadMapThumbnail(lat, lng, size)`: hitung tile Web Mercator di zoom 16, fetch grid 3x3 tile di sekitar titik (lewat proxy di atas) paralel, gabung ke offscreen canvas 768x768, lalu crop persegi tepat berpusat di titik lokasi seukuran thumbnail. Timeout 2.5 detik per tile (`Promise` reject via `setTimeout`) — kalau network lambat/gagal, watermark tetap jalan tanpa thumbnail (fallback kotak gelap polos), TIDAK memblokir proses absen.
  - Thumbnail dirender rounded-corner (`roundedRectPath` + `clip()`) dengan ring putih tipis, pin marker digambar di atas thumbnail (bukan lagi berdiri sendiri tanpa map). Ukuran teks lokasi diperbesar: `titleSize` 20→30 scale, `bodySize` 14→20 scale.
  - `AbsenPanel.tsx` — `await drawAbsenceWatermark(...)` (sebelumnya panggilan sync tanpa await).
- **Belum dilakukan**: test manual di browser sungguhan (kamera + lokasi asli + cek watermark & mini-map ter-render benar di foto hasil upload Cloudinary, termasuk kondisi network lambat untuk verifikasi fallback tile timeout) — implementasi baru diverifikasi lewat `npm run build` (30 route, 0 error) dan `npx eslint`/`npx tsc --noEmit` bersih, belum divisualkan langsung.

## Redesign Popup Ambil Foto: Full-Screen + Step Preview + "Cek Lokasi" (2026-07-19, iterasi ketiga)
Trigger: popup kamera sebelumnya kecil (card `w-72` di tengah layar) dengan preview map Leaflet live selalu tampil di bawah video — user minta kamera full-page, ada step preview (foto+lokasi) sebelum kirim dengan opsi ambil ulang, dan map live diganti tombol "Cek Lokasi" on-demand.

`src/components/AbsenPanel.tsx` dirombak total alurnya:
- `Step` sekarang 4 nilai: `"idle" | "camera" | "preview" | "submitting"` (sebelumnya cuma 3, tidak ada `"preview"`).
- **Step `camera`**: `fixed inset-0 z-[60] bg-black`, video `object-cover` mengisi seluruh viewport (bukan lagi card kecil). Map Leaflet live **dihapus total** dari step ini. Tombol capture (lingkaran putih besar) + batal di overlay bawah dengan gradient.
- **Step `preview` (BARU)**: setelah `handleCapture()`, watermark di-bake ke canvas (fungsi & urutan sama seperti sebelumnya), lalu `canvas.toBlob()` dikonversi jadi `URL.createObjectURL(blob)` dan ditampilkan **full-screen** sebagai `<img>` (bukan langsung upload). User bisa: (1) **"Ambil Ulang"** → `handleRetake()` buang blob/preview lama (`URL.revokeObjectURL`) lalu panggil ulang `startCamera()` (fresh camera+location+address, bukan reuse yang lama), atau (2) **"Kirim Check-in/Checkout"** → `handleConfirm()` baru melakukan upload Cloudinary + submit API pakai `capturedBlob` yang sudah ada (watermark sudah ter-bake, tidak digambar ulang).
- **Tombol "Cek Lokasi" (BARU, menggantikan map live)** — di step `preview`, klik buka `Modal` (`ui/Modal.tsx`) berisi `LocationMap` interaktif (`showAccuracy`, `className="h-64 w-full"`) + teks alamat lengkap (`address.displayName`) kalau ada. Ini yang menggantikan map Leaflet yang tadinya selalu tampil di bawah live camera — sekarang on-demand, bukan always-on.
- `handleStart` di-rename jadi `startCamera` (dipanggil dari tombol idle DAN dari `handleRetake`) — refactor supaya reusable, bukan cuma dipanggil sekali dari tombol awal.
- State baru: `previewUrl`, `capturedBlob`, `showLocationModal`, plus `previewUrlRef` (ref terpisah dari state supaya cleanup `URL.revokeObjectURL` di `useEffect` unmount tidak kena stale-closure).
- **Pola untuk fitur capture-foto lain ke depan (kalau ada)**: reuse pola `previewUrlRef` + `clearPreview()` untuk cleanup object URL — jangan biarkan `URL.createObjectURL` tanpa `revokeObjectURL` pasangannya (memory leak blob di browser).
- Verifikasi: `npm run build` sukses (30 route, 0 error), `npx eslint`/`npx tsc --noEmit` bersih.
- **Belum dilakukan**: test manual di browser sungguhan — alur lengkap idle→camera fullscreen→capture→preview→cek lokasi modal→ambil ulang→kirim, belum divisualkan langsung.

## Fix 3 Bug Setelah Redesign Popup Foto (2026-07-19, iterasi keempat)
User lapor 3 masalah setelah redesign full-screen+preview di atas:

1. **Modal "Cek Lokasi" tidak muncul sama sekali saat diklik** — root cause: overlay `camera`/`preview`/`submitting` di-set `z-[60]`, sedangkan `Modal` (`ui/Modal.tsx`) hardcode `z-50` di komponennya sendiri — jadi modal ke-render TAPI ketutup di belakang overlay preview (z-index lebih rendah, walau urutan DOM setelahnya). Fix: turunkan overlay `camera`/`preview`/`submitting` di `AbsenPanel.tsx` dari `z-[60]` ke `z-40` (masih di atas header/`z-20`), supaya `Modal` (`z-50`, tidak disentuh — dipakai di banyak tempat lain) otomatis tampil di atasnya. **Pola untuk overlay fullscreen baru ke depan**: kalau perlu buka `Modal` di atas overlay custom, overlay custom itu HARUS `z-40` atau lebih rendah — jangan pernah pakai z-index ≥50 untuk overlay non-Modal.
2. **Thumbnail peta watermark sering fallback jadi kotak gelap + pin doang (bukan gambar peta asli)** — root cause: `loadMapThumbnail()` di `src/lib/watermark.ts` sebelumnya SELALU fetch grid 3x3 = 9 tile lewat proxy `/api/maps/tile`, dengan timeout 2.5 detik per tile (`Promise.all` gagal total kalau 1 saja telat/gagal). 9 request paralel ke proxy yang masing-masing melakukan fetch server-side sendiri ke CARTO gampang melebihi 2.5 detik di kondisi network mobile nyata (double-hop: client→server kita→CARTO), jadi fallback ke kotak gelap jadi sering/selalu terjadi. Fix: (a) hitung bounding box tile yang BENAR-BENAR overlap area crop thumbnail (biasanya cuma 1, 2, atau 4 tile — bukan 9) berdasarkan posisi crop presisi, bukan grid tetap 3x3; (b) naikkan timeout per tile ke 6 detik. **Pola untuk fitur tile/map lain ke depan**: hitung kebutuhan tile secara presisi dari bounding box, jangan asal grid NxN yang lebih besar dari perlu — tiap tile ekstra = 1 request lagi = 1 titik kegagalan lagi.
3. **Foto hasil capture ikut orientasi native kamera (bisa landscape di webcam desktop)** — user minta selalu potret. Fix: `cropToPortrait(srcW, srcH)` (BARU, helper di `AbsenPanel.tsx`) hitung crop tengah rasio 3:4 dari frame video (mirip CSS `object-fit: cover` tapi diterapkan ke `drawImage` canvas source-rect), diterapkan di `handleCapture()` SEBELUM watermark di-bake (canvas dibuat dengan ukuran hasil crop, bukan `video.videoWidth/videoHeight` mentah lagi). `getUserMedia` constraint juga ditambah `width:{ideal:720}, height:{ideal:1280}` supaya device yang support minta stream potret duluan (bukan jaminan, makanya crop tetap wajib sebagai fallback pasti).
- Verifikasi: `npm run build` sukses (30 route, 0 error), `npx eslint`/`npx tsc --noEmit` bersih.
- **Belum dilakukan**: test manual di browser/HP sungguhan untuk konfirmasi ketiga fix (map thumbnail nampak asli, cek lokasi kebuka, foto selalu potret).

## Fitur: Izin/Cuti (2026-07-19) — MVP dari brainstorm, langsung diimplementasi

Sebelumnya tercatat sebagai "di luar scope MVP" di `projectbrief.md`. Konsep final (hasil diskusi user): **satu kategori "Izin"** saja (bukan izin/sakit/cuti terpisah), **wajib approval admin** untuk pengajuan karyawan, hari dihitung **inklusif** (`endDate - startDate + 1`, mis. 9→10 = 2 hari).

- **Collection baru `leave_requests`** — detail lengkap field di `docs/database-schema.md`. `totalDays` selalu dihitung ulang di server (`src/lib/leave.ts` → `calculateLeaveDays()`), jangan pernah percaya nilai dari client.
- **Query pattern**: hanya equality filter (`employeeId`, `status`) di Firestore, filter rentang tanggal dilakukan **client-side setelah fetch** (`getApprovedLeavesInRange()` di `src/lib/leave.ts` fetch semua leave employeeId+status=approved lalu filter overlap di JS) — sengaja menghindari composite index tambahan, konsisten dengan skala data kecil per karyawan (pola sama seperti alasan `Pagination.tsx` pakai client-side slice).
- **API**: `src/app/api/leaves/mine/route.ts` (karyawan: GET riwayat sendiri, POST ajukan baru → selalu `status: "pending"`), `src/app/api/leaves/route.ts` (admin: GET semua + filter `status`/`employeeId`, POST → admin input langsung `status: "approved"` `source: "admin"`, dipakai tombol "Tambah Izin" di Manage Absence), `src/app/api/leaves/[id]/route.ts` (admin: PATCH approve/reject dengan `reviewedBy`/`reviewedAt`/`reviewNote`, DELETE). **Tidak ada collection audit log terpisah** untuk leave (beda dari `absence_audit_logs`) — field `reviewedBy`/`reviewedAt`/`reviewNote` di document itu sendiri sudah cukup sebagai jejak akuntabilitas, keputusan sengaja untuk menghindari over-engineering di fitur baru ini.
- **Employee**: menu baru **"Izin"** di `EmployeeNav.tsx`/`BottomNav.tsx` (antara Absen dan History) → `/leave` (`src/app/leave/page.tsx` + `src/components/LeaveList.tsx`) — list riwayat + tombol "Ajukan Izin" (`Modal`), form tampilkan **live total hari** begitu kedua tanggal dipilih (dihitung client-side, direplikasi persis di 3 tempat: `LeaveList.tsx`, `AbsencesTable.tsx` form admin, dan server `calculateLeaveDays()` — kalau ada tempat ke-4 butuh hitung yang sama, pertimbangkan ekstrak ke shared util, saat ini belum karena baru 3 titik).
- **Admin**: menu baru **"Izin"** di `AdminNav.tsx`/`ADMIN_NAV_ITEMS` → `/admin/leaves` (`src/app/admin/leaves/page.tsx` + `src/components/LeavesTable.tsx`) — approval queue, default filter `status=pending`, tombol Setujui/Tolak (Tolak lewat `Modal` isi catatan opsional). Tombol **"Tambah Izin"** (admin input izin atas nama karyawan, submit ke `POST /api/leaves`, otomatis `approved`) **ditaruh di `LeavesTable.tsx` (Manage Izin), bukan di `AbsencesTable.tsx` (Manage Absence)** — keputusan eksplisit user setelah implementasi awal sempat menaruhnya di Manage Absence, dipindah supaya semua hal terkait izin (approval queue + input manual) berada di satu halaman.
- **History** (`HistoryList.tsx`): fetch tambahan ke `/api/leaves/mine`, filter `status === "approved"` yang overlap rentang tampilan (bulan/custom), digabung+diurutkan dengan record absen jadi satu timeline (card baru `LeaveCard`, badge oranye "Izin"). Leave ditampilkan **per pengajuan** (satu card untuk seluruh rentang), bukan per-hari — beda dari Report yang expand per-hari.
- **Report** (`api/report/route.ts`, `api/report/export/route.ts`, `ReportView.tsx`): `summary.totalIzin` dihitung dari `overlapDays()` (hari leave yang beririsan dengan rentang query, dipotong ke batas rentang — employee bisa punya leave yang sebagian di luar rentang report). StatCard "Total Izin" baru (4 StatCard sekarang, grid `sm:grid-cols-2 lg:grid-cols-4`). Tabel/Excel: leave di-**expand per-hari** (beda dari History) dan digabung+diurutkan dengan record absen jadi satu tabel — row leave pakai badge "Izin" + kolom alasan, kolom Excel baru "Status" & "Keterangan".
- **Icon**: `CalendarOff` (nav karyawan + badge Izin), `FileClock` (nav admin, beda dari `ClipboardList` yang sudah dipakai untuk Absence).
- Verifikasi: `npm run build` sukses (34 route, 0 error), `npx eslint src/` bersih (cuma 4 warning pre-existing tidak terkait — 1 di `api/admins/route.ts`, 3 di `AbsenMapBackground.tsx`), `npx tsc --noEmit` bersih.
- **Firestore composite index dibutuhkan** (ditemukan saat test manual, 2026-07-19): `/api/leaves/mine` (`where employeeId == X` + `orderBy createdAt desc`) 500 `FAILED_PRECONDITION` sampai index `employeeId` ASC + `createdAt` DESC dibuat di collection `leave_requests` — user klik link dari error log Firestore untuk membuatnya (pola sama seperti index `absences` sebelumnya). `/api/leaves` (admin, `where status == "pending"` + `orderBy createdAt desc`) kemungkinan besar butuh index kedua (`status` ASC + `createdAt` DESC) begitu `/admin/leaves` dibuka — belum dikonfirmasi terjadi atau belum. **Koreksi dari catatan sebelumnya**: `getApprovedLeavesInRange()` (dipakai History/Report) memang equality-only jadi aman, tapi `/api/leaves/mine` dan `/api/leaves` tetap butuh index karena keduanya pakai `orderBy` di field berbeda dari `where`.
- **Belum dilakukan**: test manual lanjutan di browser (ajukan izin karyawan → approve/reject admin → cek muncul di History & Report, termasuk kasus rentang izin yang sebagian di luar rentang Report) setelah index selesai dibuat.

## Popup Ambil Foto: Rasio Live Preview vs Hasil Disamakan + Fix Bug "Ambil Ulang" (2026-07-20)
Trigger 1: user lapor rasio live camera (full-viewport `object-cover`) beda dengan rasio hasil foto (di-crop potret 3:4 lewat `cropToPortrait()`) — apa yang dilihat user saat framing tidak sama dengan hasil akhir.
Trigger 2: user lapor tombol "Ambil Ulang" kadang tidak merespons klik, kadang kamera nongol hitam blank.

**Fix rasio (WYSIWYG)**: `src/components/AbsenPanel.tsx` — baik step `camera` (video) maupun step `preview` (`<img>` hasil) sekarang dibungkus container yang SAMA persis: `relative aspect-[3/4] max-h-full w-full max-w-[520px] overflow-hidden`, `object-cover` di dalamnya. Sebelumnya video/`<img>` full-viewport (`h-full w-full` langsung di elemen fixed inset-0), croppingnya ikut rasio layar device (beda-beda tiap device/orientasi), padahal hasil final SELALU di-crop 3:4 oleh `cropToPortrait()` — sekarang keduanya konsisten 3:4 dengan container yang sama supaya framing = hasil.

**Fix bug "Ambil Ulang" (root cause & fix)**:
- Root cause 1 — **state limbo**: `handleRetake()` lama panggil `clearPreview()` (set `previewUrl=null`) SEBELUM `startCamera()` selesai. Kondisi render `step === "preview" && previewUrl` jadi false begitu preview di-clear, tapi `step` belum berubah ke `"camera"` (masih nunggu `getUserMedia`+`getLocation` resolve) — dalam jeda itu TIDAK ADA kondisi render yang cocok (`idle`/`camera`/`preview`/`submitting` semua false) → layar blank sampai promise selesai. Kalau `getUserMedia` lambat (kamera butuh waktu re-init setelah stream lama di-stop), jeda ini kerasa seperti "diklik gaada kejadian" atau "video blank hitam".
- Root cause 2 — **race condition tanpa guard**: tidak ada mekanisme mencegah `startCamera()` dipanggil dobel (klik cepat berturut-turut) atau stream lama yang belum sempat `stop()` bentrok dengan request stream baru.
- **Fix**: `startCamera()` di `AbsenPanel.tsx` dirombak total — pakai **cancellation token** (`startTokenRef`, increment tiap panggilan): (1) `stopCamera()` dipanggil di AWAL (lepas stream lama dulu sebelum minta baru), (2) `setStep("camera")` dipanggil SEGERA (sebelum `await getUserMedia`) jadi tidak ada jeda blank — overlay kamera langsung tampil dengan state `cameraLoading=true` (spinner "Menyiapkan kamera..." menutupi video yang masih hitam), (3) setelah `Promise.all` resolve, dicek `startTokenRef.current === token` — kalau tidak cocok (kalah dari panggilan retake/cancel berikutnya), stream baru langsung di-`stop()` dan dibuang, tidak menyentuh state apa pun (mencegah race). `handleCancel()` juga increment token supaya start yang lagi in-flight otomatis dianulir kalau user keburu klik Batal.
- `handleRetake()` disederhanakan jadi `await startCamera()` saja (clearPreview sudah dilakukan di dalam `startCamera()`).
- Tombol capture (`Ambil Foto`) di-`disabled` selagi `cameraLoading` (`opacity-40`), supaya tidak bisa capture frame yang belum ada stream-nya.
- **Pola untuk flow async multi-step lain ke depan (kalau ada)**: kalau re-entrant call mungkin terjadi (user bisa klik ulang sebelum proses sebelumnya selesai), pakai pola cancellation token ini (`useRef` counter, increment tiap invocation, cek kecocokan setelah `await`) — jangan cuma andalkan urutan promise resolve, karena promise lama bisa resolve belakangan dan menimpa state dari promise baru.
- Verifikasi: `npm run build` sukses (34 route, 0 error), `npx eslint`/`npx tsc --noEmit` bersih.
- **Belum dilakukan**: test manual di browser/HP sungguhan — klik "Ambil Ulang" berkali-kali cepat, retake di koneksi/device lambat, konfirmasi tidak ada lagi blank screen atau kamera hitam.

## Watermark Foto: Perbesar Lagi (2026-07-20) — Root Cause Referensi Skala Salah
User minta thumbnail peta + teks lokasi di watermark diperbesar lagi. Root cause kenapa masih terasa kecil meski sudah dinaikkan sebelumnya: `src/lib/watermark.ts` hitung `scale = w / 1280` (referensi lebar landscape), padahal canvas foto SEKARANG SELALU potret hasil `cropToPortrait()` (`AbsenPanel.tsx`) dengan lebar native ~720-1080px — jauh di bawah 1280, jadi `scale` selalu <1 dan seluruh elemen watermark (map thumbnail, font) otomatis mengecil.
**Fix**: `scale = w / 900` (referensi lebar potret, bukan landscape) + ukuran dasar dinaikkan sekali lagi: `mapSize` 130→190, `titleSize` 30→42, `bodySize` 20→27, `padX` 18→20, `gapAfterMap` 14→16, `lineGap` 6→9 (semua dikali `scale`, elemen lain seperti radius/pin/stroke ikut otomatis membesar karena sudah pakai `* scale`).
**Pola untuk tuning ukuran watermark ke depan**: SELALU pikirkan `scale` relatif terhadap lebar canvas AKTUAL yang mungkin terjadi (sekarang potret ~720-1080px, bukan landscape 1280px) — kalau ada perubahan lain ke resolusi/orientasi capture, cek ulang referensi `scale` di `watermark.ts` supaya tidak salah proporsi lagi.
Verifikasi: `npm run build` sukses, `npx eslint`/`npx tsc --noEmit` bersih. Belum dites visual langsung di foto asli.

## Bug Fix: Filter Rentang Tanggal Salah Hari untuk Checkin 00:00-06:59 WIB (2026-07-20)

**Gejala** (dilaporkan user): checkin jam 00:49 WIB tanggal 20 Juli tidak muncul saat filter Report di-set rentang tanggal 20-20 Juli, tapi muncul kalau tanggal mulai diubah ke 19 Juli.

**Root cause**: semua endpoint yang filter `checkinTime` berdasarkan `startDate`/`endDate` (format `yyyy-MM-dd`) membangun batas rentang lewat `new Date(\`${dateStr}T00:00:00.000Z\`)`/`T23:59:59.999Z` — ini memperlakukan tanggal sebagai **UTC**, padahal `checkinTime` disimpan sebagai instan UTC dari waktu **WIB (Asia/Jakarta, UTC+7)** sebenarnya. Checkin jam 00:49 WIB 20 Juli = `2026-07-19T17:49:00Z` — jatuh SEBELUM batas `2026-07-20T00:00:00.000Z`, jadi ke-exclude dari rentang 20-20; baru masuk kalau rentang diperlebar ke 19 Juli. Bug ini sistemik: setiap checkin jam 00:00-06:59 WIB salah dianggap masuk hari kalender sebelumnya di **semua** filter rentang tanggal di app (6 titik ditemukan lewat grep).

**Fix**: `src/lib/date.ts` ditambah 5 helper baru — `startOfJakartaDayUtc(dateStr)`/`endOfJakartaDayUtc(dateStr)` (bangun batas hari via offset literal `+07:00`, bukan `Z`, karena WIB tidak pernah DST jadi aman pakai offset tetap), `jakartaMonthRangeUtc(year, month)` (batas bulan kalender WIB, dipakai fallback filter per-bulan), `formatJakartaDateId(date)`/`formatJakartaTimeId(date)` (format tampilan eksplisit timezone WIB, tidak bergantung timezone server). Diterapkan di **6 titik**: `api/absences/route.ts`, `api/report/route.ts`, `api/report/export/route.ts` (dua tempat: batas rentang query DAN loop cursor per-hari expand leave — sebelumnya `cursor.toISOString().slice(0,10)` juga salah hari karena cursor dibangun via `Z`, sekarang pakai `jakartaDateString()`; format tanggal/jam Excel juga diubah dari `toLocaleDateString("id-ID")` tanpa timezone ke `formatJakartaDateId`/`formatJakartaTimeId` eksplisit — server production bisa jalan di timezone UTC, bukan WIB), `api/absences/mine/route.ts` (dua cabang: custom range DAN fallback per-bulan yang sebelumnya pakai `Date.UTC` langsung).

**Pola wajib untuk filter/format tanggal baru ke depan**: JANGAN PERNAH bangun batas rentang tanggal dari string `yyyy-MM-dd` langsung pakai `T00:00:00.000Z`/`T23:59:59.999Z` atau `Date.UTC(...)` — selalu pakai `startOfJakartaDayUtc()`/`endOfJakartaDayUtc()`/`jakartaMonthRangeUtc()` dari `src/lib/date.ts`. Untuk format tampilan tanggal/jam dari `checkinTime`/`checkoutTime` di server (bukan client component), pakai `formatJakartaDateId()`/`formatJakartaTimeId()`, bukan `toLocaleDateString`/`toLocaleTimeString` tanpa opsi `timeZone`.

Verifikasi: `npx tsc --noEmit` bersih, `npx eslint` bersih (file yang diubah), `npm run build` sukses (34 route, 0 error). **Belum dilakukan**: test manual — cek checkin dini hari WIB (00:00-06:59) sekarang muncul benar di filter Report/Manage Absence/History untuk hari yang tepat.

## Fix: Thumbnail Peta Kadang Nampak Polos Tanpa Jalan (2026-07-20)
User lapor thumbnail peta di watermark kadang cuma nampak kotak warna tanah/gedung polos (cream/beige, warna dasar basemap CARTO Voyager) + pin, tanpa garis jalan atau landmark — **bukan** karena kualitas foto (thumbnail peta digambar terpisah dari JPEG kamera, tidak terpengaruh setting kualitas `q_auto:good` Cloudinary). Dikonfirmasi tile CARTO sendiri berfungsi normal (dites langsung fetch tile area ramai, hasilnya jelas ada jalan/gedung).

**Root cause**: crop thumbnail sebelumnya persis seukuran tampil (`mapSize`, ~150-300px tergantung resolusi foto) di zoom 16 — itu setara area tanah cuma ~250-450m. Kalau titik lokasi user pas ada di tengah halaman/plaza/gedung besar yang jalannya tidak melintasi window sesempit itu, thumbnail ya cuma nampak warna dasar tanah polos (bukan bug rendering, memang area itu kosong secara visual di radius sekecil itu).

**Fix**: `src/lib/watermark.ts` — `loadMapThumbnail()` sekarang crop area **2.2x lebih lebar** (`ZOOM_OUT_FACTOR = 2.2`) dari ukuran tampil thumbnail, lalu `drawImage` di pemanggil (`ctx.drawImage(mapThumb.canvas, ..., mapThumb.s, mapThumb.s, ..., mapSize, mapSize)`) otomatis scale-down crop yang lebih lebar itu ke ukuran kotak thumbnail — efek "zoom out" gratis tanpa mengubah tampilan zoom tile sumber (masih z16, tetap tajam), cuma area yang di-capture lebih luas (~550-1000m tergantung resolusi foto) sehingga jauh lebih mungkin menangkap jalan/landmark di sekitar titik lokasi.
**Pola untuk tuning peta lain ke depan**: kalau masih terlalu polos di lokasi tertentu, naikkan `ZOOM_OUT_FACTOR` lagi (trade-off: makin besar factor = makin banyak konteks tapi makin "kecil"/kurang detail titik presisi lokasi terlihat) — jangan turunkan `MAP_ZOOM` (bikin tile blur saat di-scale up).
Verifikasi: `npm run build` sukses, `npx eslint`/`npx tsc --noEmit` bersih. Belum dites visual di lokasi asli yang dilaporkan user (tidak tahu koordinat pastinya).

## Tombol Batal di Step Preview (2026-07-20)
User minta ada tombol cancel absen di step preview (foto sudah diambil, belum dikirim) — sebelumnya cuma ada "Ambil Ulang" (retake) dan "Kirim", tidak ada cara batal total balik ke idle tanpa retake dulu.
`AbsenPanel.tsx` — baris tombol bawah di step `preview` sekarang 3 tombol: **Batal** (icon `X` polos, panggil `handleCancel` yang sudah ada — stop kamera/stream, buang preview, balik ke `step="idle"`), **Ambil Ulang** (icon `RotateCcw`), **Kirim Check-in/Checkout** (tombol utama). Batal & Ambil Ulang di-render sebagai **tombol bulat icon-only** (`h-12 w-12`, tanpa label teks, `aria-label` untuk aksesibilitas) — bukan pill dengan teks seperti sebelumnya, supaya 3 tombol muat rapi di layar sempit tanpa wrap/overflow (Kirim tetap pill teks lengkap sebagai CTA utama yang paling menonjol).
Verifikasi: `npm run build` sukses, `npx eslint`/`npx tsc --noEmit` bersih.

## Fitur: Toggle Kamera Depan/Belakang saat Check-in/Checkout (2026-07-21)
User minta bisa pakai kamera belakang juga (sebelumnya hardcode `facingMode: "user"` alias kamera depan saja).
`AbsenPanel.tsx` — state baru `facingMode` (`"user" | "environment"`, default `"user"` — tetap selfie sebagai default karena itu kebutuhan utama absen). Tombol baru **"Ganti Kamera"** (icon `SwitchCamera` dari lucide-react) di overlay step `camera`, menggantikan spacer kosong di sisi kanan tombol capture. `switchCamera()` (BARU) **hanya** minta ulang `getUserMedia` dengan `facingMode` yang ditoggle — **tidak** request ulang Geolocation (lokasi yang sudah didapat di `startCamera()` dipertahankan), pakai pola cancellation token (`startTokenRef`) yang sama seperti `startCamera()`/`handleRetake()` supaya aman dari race kalau user klik cepat berturut-turut atau ganti kamera lalu langsung batal.
`startCamera()` sekarang baca `facingMode` dari state (bukan hardcode `"user"`) — jadi kalau user retake setelah switch ke kamera belakang, kamera belakang yang dipakai lagi (state persist lintas retake dalam satu sesi buka panel, reset ke `"user"` lagi kalau komponen di-remount/reload halaman).
Verifikasi: `npx tsc --noEmit` bersih, `npx eslint src/components/AbsenPanel.tsx` bersih. **Belum dilakukan**: test manual di HP sungguhan (kamera belakang benar-benar aktif, watermark & crop potret tetap benar dari kamera belakang, switch tidak nge-hang).

## Fitur: Foto & Lokasi di Report Harian + Link Foto/Lokasi di Semua Export Excel (2026-07-21)
User minta 2 hal: (1) tabel Report Harian (`DailyReportView.tsx`, tab "Rekap Harian") tampilkan foto & lokasi per karyawan (sebelumnya cuma status/checkin/checkout/keterangan, tidak ada sama sekali), (2) export Excel Report Harian **dan** Report Per Karyawan (`ReportView.tsx`, tab "Per Karyawan") sertakan link foto & lokasi (sebelumnya export cuma teks tanggal/status/jam, tidak ada link apa pun walau UI tabelnya — khusus Per Karyawan — sudah tampilkan tombol foto/lokasi).

- **`api/report/daily/route.ts`**: `absenceByEmployee` map & response `employees[]` sekarang bawa `checkinLocation`/`checkinPhotoUrl`/`checkoutLocation`/`checkoutPhotoUrl` (sebelumnya cuma `checkinTime`/`checkoutTime`).
- **`DailyReportView.tsx`**: kolom baru "Lokasi" & "Foto" di tabel (persis pola `ReportView.tsx` yang sudah ada duluan — tombol "In"/"Out" buka `Modal`+`LocationMap` untuk lokasi, lightbox `<img>` fullscreen untuk foto). **Pola untuk tabel report lain ke depan yang butuh tampilkan foto/lokasi**: reuse pola ini (state `zoomPhoto`+`mapModal`, helper `mapsLink()`), jangan duplikasi/reinvent.
- **`api/report/daily/export/route.ts`** & **`api/report/export/route.ts`**: 4 kolom baru di Excel — "Foto Checkin", "Foto Checkout", "Lokasi Checkin", "Lokasi Checkout" — masing-masing cell berisi **hyperlink ExcelJS sungguhan** (`{text: "Lihat Foto"/"Lihat Lokasi", hyperlink: url}`, bukan teks URL polos), font biru+underline (`HYPERLINK_FONT` const, disamakan gayanya di kedua file) supaya user Excel tinggal klik. `-` kalau tidak ada foto/lokasi (row izin/alpha). Link lokasi pakai format `https://www.google.com/maps?q=lat,lng` (`mapsLink()` helper, direplikasi di kedua route export — sudah ada pola sama di `ReportView.tsx` client-side, ini versi server-side untuk Excel).
- **Tidak disentuh**: `MonthlyReportView.tsx`/`api/report/monthly*` (tab "Rekap Bulanan") — cuma agregat total hadir/izin per karyawan per bulan, tidak ada record individual per-hari jadi tidak relevan untuk foto/lokasi per definisi fitur ini.
- **Pola wajib untuk kolom link baru di Excel export ke depan**: selalu pakai cell value object `{text, hyperlink}` + `cell.font = HYPERLINK_FONT` (bukan taruh URL mentah sebagai string) supaya konsisten clickable di semua export.
- Verifikasi: `npx tsc --noEmit` bersih, `npx eslint` bersih (4 file yang diubah), `npm run build` sukses (38 route, 0 error).
- **Belum dilakukan**: test manual buka file Excel hasil export di Excel/Sheets sungguhan untuk konfirmasi link foto/lokasi benar-benar clickable dan mengarah ke URL yang tepat; test tabel Report Harian di browser (klik tombol foto/lokasi, modal & lightbox tampil benar).

## Links
- Lihat `progress.md` untuk breakdown fase & checklist lengkap.
- Dokumen sumber detail: `docs/tech-stack.md`, `docs/features.md`, `docs/database-schema.md`, `docs/cloudinary-schema.md`.
